import AuthForm from "@/components/auth/AuthForm";

const page = () => {
    return (
        <div className=''>
            <AuthForm
                type="sign-in"
            />
        </div>
    );
};

export default page;